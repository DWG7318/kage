# 实测与验证记录

## 基线

- 上游提交：`4399487d2fb42bce39c7b032fbbb50d230bf4f0b`
- 提交日期：2026-08-09
- 本次核查日期：2026-08-31
- 本地入口：`http://127.0.0.1:4173/`
- 项目有效载荷：约 3.63MB（不含 Git 与研究产物）

## 资产清单

- 4 张章节场景 WebP：1024–1586px 级，无 alpha；
- 10 张透明前景 WebP：1024–1774px 级，RGBA；
- 1 张预览图；
- 本地字体 CSS；
- vendored Three.js r149；
- 单个 `index.html`，约 4,821 行、249KB。

## 浏览器实测

- 页面加载后存在 4 个 canvas：主 Three.js 画布 + 3 个卡片织物画布；
- 当前章节的 HTML 前景会被挂到 `#fg-sky`；
- 章节切换后 80ms 与 440ms 时同时存在新 `fg-active` 和旧 `fg-retiring`；
- 约 960ms 后旧前景已回到原 section；
- 卡片悬停时箭头 opacity 为 1，自定义光标从 26px 扩到 52px；
- 课程项悬停时编号变为朱红，底线 transform 达到完整展开；
- 画面确认存在鼠标微粒、落叶、卡片织物起伏、月亮/灯笼呼吸与摄影机视差；
- 主页面与架构面板均返回 HTTP 200；
- 原始受控源码在分析完成后 `git diff --name-only` 为空。

## 自动知识图谱

- 文件节点：21；
- 明确关系：3；
- 架构层：3；
- 阅读导览：7 步；
- 最终引用校验：0 issues；
- 限制：HTML 结构分析器未把内嵌 JavaScript 函数展开成节点，因此函数级理解以人工深度分析和实现解剖为准。

## 已核对的降级路径

- `prefers-reduced-motion`；
- coarse pointer；
- low quality query 参数；
- 自适应 render scale；
- 页面隐藏暂停；
- 卡片屏外暂停；
- WebGL 初始化失败 fallback。

这些记录用于说明本研究观察到的行为，不是跨浏览器认证报告。正式客户项目仍需在目标设备矩阵上重新验证。
